{
    "reset_commands": {
        "description": "重置后清空分叉点的命令",
        "type": "list",
        "default": [
            "/reset",
            "/new"
        ],
        "hint": "这些命令的回复发送完成后，本插件会清空当前聊天窗口内保存的所有分叉点，并删除不再使用的分叉对话。"
    }
}
